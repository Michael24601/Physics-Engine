/*
	Header file for adapter used to connect two positions together. The
	adapter ensures two Vector3D positions remain the same, even if each
	is moved through different mechanisms.
	We can't just set one to be another as that would ignore either the
	first or the second'd movement. Instead, we can calculate how much
	the first and second moved seperately, then apply the translation to
*/

#ifndef VECTOR_3D_ADAPTER_H
#define VECTOR_3D_ADAPTER_H

namespace pe {
	clas 
}

#endif